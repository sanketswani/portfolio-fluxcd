apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: test-flux
  namespace: ite
spec:
  interval: 1m
  chart:
    spec:
      chart: test-flux
      version: "0.1.0"
      sourceRef:
        kind: HelmRepository
        name: test-flux-repo
        namespace: default